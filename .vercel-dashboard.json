{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @reachdem/database db:generate && pnpm --filter dashboard build",
  "outputDirectory": "apps/dashboard/.next"
}
